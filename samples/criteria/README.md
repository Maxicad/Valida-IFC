# Exemplos de criterios

`criterios_exemplo.csv` e `criterios_exemplo.txt` servem como base para a importacao do MVP.

O arquivo `.xlsx` sera gerado quando a rotina de importacao/exportacao com OpenPyXL for implementada.
