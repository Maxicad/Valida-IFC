# Valida IFC Web

Frontend Next.js com App Router, TypeScript e Tailwind CSS.

## Executar

```powershell
corepack enable
corepack prepare pnpm@10.12.1 --activate
pnpm install
pnpm --filter @valida-ifc/web dev
```

Variavel principal:

```text
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
```
