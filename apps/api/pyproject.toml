[project]
name = "valida-ifc-api"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[tool.pytest.ini_options]
pythonpath = [
  ".",
  "../../packages/rules-engine",
  "../../packages/ifc-utils"
]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py312"
